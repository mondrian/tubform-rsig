<h1>Listing lojas</h1>

<table>
  <tr>
    <th>Nome loja</th>
  </tr>

<% @lojas.each do |loja| %>
  <tr>
    <td><%=h loja.nome_loja %></td>
    <td><%= link_to 'Show', loja %></td>
    <td><%= link_to 'Edit', edit_loja_path(loja) %></td>
    <td><%= link_to 'Destroy', loja, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New loja', new_loja_path %>