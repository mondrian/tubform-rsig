<h1>Alterando pedido</h1>

<% form_for(@pedido) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Salvar' %>
  </p>
<% end %>
<%= button_to 'Aprovar', :action => :aprovar, :id => @pedido.id %>
<%= link_to 'Exibir', @pedido %> |
<%= link_to 'Voltar', pedidos_path %>

